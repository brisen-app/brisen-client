name: 🚀 Push Preview

on:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  update-preview:
    name: 🔨 Build Preview
    runs-on: ubuntu-latest
    environment: development
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      EXPO_PROJECT: ${{ vars.EXPO_PROJECT }}
    steps:
      - name: 🏗 Setup repo
        uses: actions/checkout@v3

      - name: 🏗 Setup Node
        uses: actions/setup-node@v3

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          packager: npm
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Install dependencies
        run: npm install

      - name: 🚀 Create preview
        uses: expo/expo-github-action/preview@v8
        env:
          EXPO_PUBLIC_SB_URL: ${{ vars.EXPO_PUBLIC_SB_URL }}
          EXPO_PUBLIC_SB_ANON: ${{ secrets.EXPO_PUBLIC_SB_ANON }}
        with:
          command: eas update --auto --branch ${{ github.event.pull_request.head.ref }} --message ${{ github.event.pull_request.title }}
