name: Check labels

on:
  pull_request:
    types:
      - opened
      - labeled
      - unlabeled

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check-labels:
    runs-on: ubuntu-latest
    steps:
      - name: Print labels array
        run: |
          echo "Labels Array: '${{ toJson(github.event.pull_request.labels) }}'"
          exit 1
